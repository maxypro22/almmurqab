/* Generated by npm run build:static from lib/quickAnswers.ts — do not edit here. */
(function () {
  "use strict";
  var D = window.SITE_DATA;
  var firm = D.firm, about = D.about, faqs = D.faqs, practiceAreas = D.practiceAreas;
  function getPracticeArea(slug) { return practiceAreas.find(function (a) { return a.slug === slug; }); }
  function publishedArticles() { return D.articles; }
  function publishedTeam() { return D.team; }




/**
 * The assistant, in full. There is no language model behind this — every
 * answer below is composed from content/firm.ts and its siblings, matched by
 * keyword, and it runs in the visitor's browser.
 *
 * That is a deliberate choice, not a limitation to apologise for:
 *
 *  - **The facts cannot drift.** "Where are you?" and "when are you open?"
 *    have exactly one correct answer and it is already in the content files.
 *    Routing that through a language model adds paraphrase risk for no gain —
 *    and on a law firm's site, a confidently wrong opening hour or a
 *    hallucinated practice area is a real cost.
 *  - **Nothing can be unavailable.** No API key, no network call, no rate
 *    limit, no outage, no bill. The widget cannot show an error, because
 *    there is nothing to fail.
 *  - **It is honest about its range.** When it does not know, it says so and
 *    hands over the phone number. It never guesses, because it has nothing to
 *    guess with.
 *
 * The one thing it must never do is answer a legal question. It doesn't: the
 * intents below cover the office, not the law, and anything it cannot place
 * goes to `unmatchedAnswer` — which points at a human.
 */

/**
 * Long messages are not keyword-matched.
 *
 * Someone typing three lines about their arrest must not receive a canned
 * reply about opening hours because the word "time" appeared. Past this
 * length the assistant stops guessing at intent and hands over to a person,
 * which is the right outcome for anything that detailed anyway.
 */
const MAX_QUICK_CHARS = 90;

             
            
          
              
           
             
               
          
               
           
              
           

/** Keywords are matched against a lowercased, punctuation-stripped message. */
const INTENTS                                           = [
  // Checked first on purpose: an urgent message must reach a human, and must
  // not be intercepted by the word "police" matching criminal law.
  {
    intent: "urgent",
    keywords: [
      "urgent", "emergency", "arrested", "arrest", "detained", "detention", "custody now",
      "عاجل", "طارئ", "توقيف", "موقوف", "القبض", "احتجاز", "محتجز",
    ],
  },
  {
    intent: "fees",
    keywords: [
      "fee", "fees", "cost", "costs", "price", "how much", "charge", "quote", "rate",
      // No bare "كم" here: it opens half the questions a visitor might ask
      // ("كم عدد مجالاتكم"), so the fee intent needs a fee word.
      "أتعاب", "تكلفة", "سعر", "الأسعار", "كم تكلف", "كم التكلفة", "كم الأتعاب",
    ],
  },
  {
    intent: "location",
    keywords: [
      "where are you", "where is", "located", "location", "address", "directions", "map",
      "how do i get", "find you", "office is",
      "أين", "موقع", "الموقع", "عنوان", "العنوان", "وين",
    ],
  },
  {
    intent: "hours",
    keywords: [
      "hours", "opening", "open", "closed", "close", "what time", "when are you",
      "working days", "weekend",
      "ساعات", "مواعيد", "الدوام", "متى تفتح", "متى", "أوقات", "العمل من",
    ],
  },
  {
    intent: "booking",
    keywords: [
      "book", "booking", "appointment", "consultation", "consult", "contact", "reach you",
      "phone", "telephone", "number", "email", "whatsapp", "call you",
      "احجز", "حجز", "موعد", "استشارة", "تواصل", "اتصال", "رقم", "هاتف", "بريد", "إيميل", "واتساب",
    ],
  },
  {
    intent: "areasList",
    keywords: [
      "practice areas", "areas of law", "services", "what do you do", "what areas",
      "specialis", "specializ", "types of cases",
      "مجالات", "المجالات", "خدمات", "الخدمات", "تخصص", "التخصصات",
    ],
  },
  {
    intent: "team",
    keywords: [
      "lawyer", "lawyers", "advocate", "advocates", "attorney", "partner", "partners",
      "who works", "your team", "staff",
      // Arabic possessive and plural forms are listed out rather than stemmed.
      // "محاموكم" (your lawyers) is three letters away from "محامي", past what
      // the inflection tolerance in `matches` allows — and widening that
      // tolerance to catch it would start matching unrelated words.
      "محامي", "محامين", "محامون", "محاموكم", "محامينا", "شركاء", "الفريق", "فريقكم",
    ],
  },
  {
    intent: "languages",
    keywords: [
      "language", "languages", "english", "arabic", "speak english", "speak arabic",
      "لغة", "لغات", "الانجليزية", "الإنجليزية", "العربية",
    ],
  },
  {
    intent: "articles",
    keywords: [
      "article", "articles", "blog", "insights", "news", "publications",
      "مقال", "مقالات", "المقالات", "مدونة", "أخبار",
    ],
  },
  {
    intent: "about",
    keywords: [
      "about the firm", "about you", "who are you", "tell me about", "your experience",
      "how long have you", "established",
      "عن المكتب", "من انتم", "من أنتم", "نبذة", "خبرتكم", "تأسس",
    ],
  },
];

/**
 * Keywords per practice area, so "do you handle labour cases?" resolves to the
 * Labour Law page rather than a generic list. Derived by hand because the
 * words a visitor uses are not the words in a practice-area title — nobody
 * searches for "Labour Law", they ask about "end of service".
 */
const AREA_KEYWORDS                           = {
  labor: [
    "labour", "labor", "employment", "employee", "employer", "worker", "wage", "salary",
    "end of service", "gratuity", "resign", "dismissal", "sponsor",
    "عمل", "العمل", "عمالي", "عمالية", "وظيفة", "راتب", "مكافأة", "استقالة", "فصل",
  ],
  criminal: [
    "criminal", "crime", "police", "prosecution", "bail", "charged", "accused",
    "جنائي", "جنائية", "جزائي", "جريمة", "نيابة", "تهمة", "متهم",
  ],
  civil: [
    "civil", "compensation", "damages", "debt", "contract dispute",
    "مدني", "مدنية", "تعويض", "دين", "مطالبة",
  ],
  commercial: [
    "commercial", "trade", "trading", "merchant", "supplier",
    "تجاري", "تجارية", "تجارة", "تاجر",
  ],
  corporate: [
    "corporate", "company", "companies", "incorporat", "shareholder", "llc", "branch",
    "foreign ownership", "شركات", "الشركات", "شركة", "تأسيس", "شريك", "حصص",
  ],
  administrative: [
    "administrative", "government", "ministry", "public body", "licence", "license",
    "إداري", "إدارية", "حكومي", "وزارة", "ترخيص",
  ],
  "real-estate": [
    "real estate", "realestate", "property", "landlord", "tenant", "rent", "lease", "villa",
    "عقار", "عقاري", "عقارية", "إيجار", "مالك", "مستأجر",
  ],
  family: [
    "family", "divorce", "custody", "marriage", "alimony", "inheritance", "khula",
    "أسري", "أسرية", "أسرة", "طلاق", "حضانة", "زواج", "نفقة", "ميراث", "خلع",
  ],
  arbitration: [
    "arbitration", "arbitral", "arbitrator", "qicca", "icc",
    "تحكيم", "التحكيم", "محكم",
  ],
};

function normalise(text        )         {
  return text
    .toLowerCase()
    // Strip Arabic diacritics and normalise alef/ya variants so "أين" and
    // "اين" both match.
    .replace(/[ً-ْ]/g, "")
    .replace(/[إأآ]/g, "ا")
    .replace(/ى/g, "ي")
    .replace(/ة/g, "ه")
    .replace(/[^\p{L}\p{N}\s]/gu, " ")
    .replace(/\s+/g, " ")
    .trim();
}

/**
 * Token-aware matching. A plain `includes()` is wrong here, and Arabic is
 * where it shows: "مكتبكم" (your office) ends in the letters of "بكم" (how
 * much), so "وين مكتبكم" — where is your office — came back with the fees
 * answer. Short Arabic words turn up inside longer unrelated ones constantly.
 *
 * So single words match whole tokens, allowing for the definite article and a
 * short inflectional tail ("arrest" → "arrested", "عمل" → "العمل"). Only
 * multi-word phrases fall back to a substring test, where the extra words
 * make a collision implausible.
 */
function matches(haystack        , keywords          )          {
  const tokens = haystack.split(" ");
  return keywords.some((raw) => {
    const key = normalise(raw);
    if (key.length < 3) return false;
    if (key.includes(" ")) return haystack.includes(key);
    return tokens.some(
      (t) =>
        t === key ||
        t === `ال${key}` ||
        (t.startsWith(key) && t.length - key.length <= 2) ||
        (t.startsWith(`ال${key}`) && t.length - key.length <= 4),
    );
  });
}

// ---------------------------------------------------------------------------
// Answers. Every value below is read from firm.ts — nothing is retyped here.
// ---------------------------------------------------------------------------

function contactLine(locale        )         {
  return locale === "ar"
    ? `هاتف: ${firm.phone.display.ar} · واتساب: نفس الرقم · البريد: ${firm.email}`
    : `Phone: ${firm.phone.display.en} · WhatsApp: the same number · Email: ${firm.email}`;
}

function answer(intent        , locale        , slug         )         {
  const ar = locale === "ar";

  switch (intent) {
    case "urgent":
      // Phone first, before anything else. Everything else can wait.
      return ar
        ? `اتصل بنا الآن على ${firm.phone.display.ar}. الاستشارات العاجلة متاحة على مدار الساعة، وسيتحدّث معك أحد المحامين مباشرة — لا تنتظر رداً من هذه المحادثة.`
        : `Call us now on ${firm.phone.display.en}. Urgent consultations are available 24/7 and an advocate will speak with you directly — please don't wait on this chat.`;

    case "fees":
      return ar
        ? `لا نعلن أتعاباً ثابتة، لأنها تعتمد على طبيعة الملف ونطاق العمل المطلوب. يُحدَّد ذلك في الاستشارة الأولى بشكل واضح قبل أي التزام. للاستفسار: ${contactLine("ar")}`
        : `We don't publish fixed fees — they depend on the nature of the matter and the scope of work involved. That is set out clearly in the first consultation, before any commitment. To ask: ${contactLine("en")}`;

    case "location":
      return ar
        ? `المكتب في ${firm.address.street.ar}، ${firm.address.city.ar}، ${firm.address.country.ar}. تجد الخريطة وتفاصيل الوصول في صفحة اتصل بنا: /${locale}/contact`
        : `Our office is at ${firm.address.street.en}, ${firm.address.city.en}, ${firm.address.country.en}. The map and directions are on the contact page: /${locale}/contact`;

    case "hours":
      return ar
        ? `${firm.hours.ar}. إن كان الأمر عاجلاً فاتصل على ${firm.phone.display.ar} في أي وقت.`
        : `${firm.hours.en}. If the matter is urgent, call ${firm.phone.display.en} at any hour.`;

    case "booking":
      return ar
        ? `أسرع طريقة هي الاتصال أو مراسلتنا على واتساب: ${firm.phone.display.ar}. أو أرسل تفاصيل طلبك عبر صفحة اتصل بنا /${locale}/contact أو على ${firm.email}، وسنعود إليك لتحديد موعد.`
        : `The quickest way is to call or message us on WhatsApp: ${firm.phone.display.en}. You can also send the details through the contact page /${locale}/contact or to ${firm.email}, and we'll come back to you with a time.`;

    case "areasList": {
      const list = practiceAreas.map((a) => a.title[locale]).join(ar ? "، " : ", ");
      return ar
        ? `نعمل في ${practiceAreas.length} مجالات: ${list}. تفاصيل كل مجال في /${locale}/practice-areas`
        : `We work across ${practiceAreas.length} areas: ${list}. Details for each are at /${locale}/practice-areas`;
    }

    case "team": {
      // Reads the same publication guard the team pages use. While the
      // profiles are placeholders it must not name anyone — inventing a
      // lawyer at a real firm is the one unrecoverable mistake here.
      const people = publishedTeam();
      if (people.length === 0) {
        return ar
          ? `صفحات المحامين لم تُنشر بعد على الموقع. عند تواصلك سيُحال ملفك إلى المحامي المختص في مجاله — ${contactLine("ar")}`
          : `Our lawyer profiles aren't published on the site yet. When you get in touch, your matter goes to the advocate who handles that field — ${contactLine("en")}`;
      }
      const list = people.map((p) => `${p.name[locale]} (${p.role[locale]})`).join(ar ? "، " : ", ");
      return ar
        ? `${list}. الملفات كاملة في /${locale}/team`
        : `${list}. Full profiles are at /${locale}/team`;
    }

    case "languages":
      return ar
        ? `نعمل بالعربية والإنجليزية، والموقع متاح بكلتا اللغتين.`
        : `We work in Arabic and English, and this site is available in both.`;

    case "articles": {
      const live = publishedArticles();
      if (live.length === 0) {
        return ar
          ? `لا توجد مقالات منشورة بعد. للاستفسار عن مسألة محددة: ${contactLine("ar")}`
          : `No articles are published yet. To ask about something specific: ${contactLine("en")}`;
      }
      return ar
        ? `لدينا ${live.length} مقالات منشورة في /${locale}/insights.`
        : `We have ${live.length} published articles at /${locale}/insights.`;
    }

    case "about":
      return ar
        ? `${about.intro.ar} المزيد في /${locale}/about`
        : `${about.intro.en} More at /${locale}/about`;

    case "area": {
      const area = slug ? getPracticeArea(slug) : undefined;
      if (!area) return answer("areasList", locale);
      return ar
        ? `نعم — ${area.title.ar} أحد مجالات عملنا. ${area.summary.ar} التفاصيل هنا: /${locale}/practice-areas/${area.slug}. أما وضعك تحديداً فيحتاج رأي محامٍ، لا رداً آلياً — ${contactLine("ar")}`
        : `Yes — ${area.title.en} is one of our practice areas. ${area.summary.en} Details here: /${locale}/practice-areas/${area.slug}. Your specific situation needs an advocate's judgment rather than an automated answer — ${contactLine("en")}`;
    }
  }
}

/**
 * What the assistant says when it cannot place the question.
 *
 * Deliberately not phrased as a failure. "I can't reach the assistant" reads
 * as a broken website; this reads as a receptionist who does not have that to
 * hand and is passing you to someone who does — which is exactly what is
 * happening, and is a good outcome for a law firm.
 */
function unmatchedAnswer(locale        )         {
  return locale === "ar"
    ? `هذا خارج ما أستطيع الإجابة عنه هنا — أنا مساعد بسيط لمعلومات المكتب فقط، ولا أُقدّم رأياً قانونياً. أقرب طريق للإجابة هو التحدث مع أحد محامينا: ${contactLine("ar")} · أو صفحة اتصل بنا /${locale}/contact`
    : `That's outside what I can answer here — I'm a simple assistant for firm information, and I don't give legal opinions. The quickest way to a real answer is one of our advocates: ${contactLine("en")} · or the contact page /${locale}/contact`;
}

/**
 * Matches a question to an answer, or returns null when nothing fits — the
 * caller then uses `unmatchedAnswer`.
 *
 * Every message is matched on its own merits, with no regard for the ones
 * before it. An earlier version only matched the *first* message of a
 * conversation, on the reasoning that a follow-up like "and the hours?"
 * depends on context. That was inherited from a design with a model behind
 * it, and here it was simply a bug: it meant the second question a visitor
 * asked, and every question after it, got "that's outside what I can answer".
 *
 * Statelessness is the honest behaviour for a keyword assistant. It has no
 * memory, so it should not pretend to reason about one.
 */
function quickAnswer(message        , locale        )                {
  const trimmed = message.trim();
  if (trimmed.length === 0 || trimmed.length > MAX_QUICK_CHARS) return null;

  const text = normalise(trimmed);

  // An exact FAQ question — the chips in the chat window send these verbatim.
  const faq = faqs.find((f) => normalise(f.q[locale]) === text);
  if (faq) return faq.a[locale];

  for (const { intent, keywords } of INTENTS) {
    if (!matches(text, keywords)) continue;

    // A named field of law takes over for `booking` alone — "I'd like a
    // consultation about a divorce" is better served by the family-law answer
    // than by generic contact details.
    //
    // Everything else keeps its own answer. Two Arabic questions proved why:
    // "ما ساعات العمل" (what are your working hours) and "ما مجالات عملكم"
    // (what are your practice areas) both contain عمل, and both were being
    // answered with the labour-law page.
    if (intent === "booking") {
      const areaSlug = Object.keys(AREA_KEYWORDS).find((s) =>
        matches(text, AREA_KEYWORDS[s]),
      );
      if (areaSlug) return answer("area", locale, areaSlug);
    }
    return answer(intent, locale);
  }

  // No generic intent, but the visitor named a field of law.
  const areaSlug = Object.keys(AREA_KEYWORDS).find((s) => matches(text, AREA_KEYWORDS[s]));
  if (areaSlug) return answer("area", locale, areaSlug);

  return null;
}

/** The single entry point the chat widget uses. Always returns something. */
function assistantReply(message        , locale        )         {
  return quickAnswer(message, locale) ?? unmatchedAnswer(locale);
}

  window.assistantReply = assistantReply;
})();
